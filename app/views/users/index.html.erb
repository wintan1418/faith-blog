<% content_for(:title) { "People - Brethreign" } %>

<div class="mystic-shell py-8">
  <div class="mystic-container max-w-3xl">
    <div class="mb-8">
      <span class="mystic-kicker">Community</span>
      <h1 class="mystic-title mt-3 text-4xl sm:text-5xl">Find people walking the same road.</h1>
      <p class="mystic-copy mt-2 max-w-2xl">Browse members, search by username or profile details, then follow or connect from their profile.</p>
    </div>

    <%= form_with url: people_path, method: :get, class: "mystic-panel-strong mb-6 p-4" do %>
      <div class="flex flex-col gap-3 sm:flex-row">
        <div class="relative min-w-0 flex-1">
          <%= render_icon("search", classes: "absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[var(--fc-muted)]") %>
          <input type="text"
                 name="q"
                 value="<%= @query %>"
                 class="input pl-10"
                 placeholder="Search username, bio, location, faith background...">
        </div>
        <button type="submit" class="fc-btn fc-btn-primary justify-center">Search People</button>
        <% if @query.present? %>
          <%= link_to "Clear", people_path, class: "btn btn-secondary justify-center" %>
        <% end %>
      </div>
    <% end %>

    <% if @users.any? %>
      <ul class="fc-people-list">
        <% @users.each do |user| %>
          <li class="fc-people-row">
            <%= link_to user_path(user.username), class: "fc-people-avatar" do %>
              <% if user.profile&.avatar&.attached? %>
                <%= image_tag user.profile.avatar, class: "object-cover", alt: user.username %>
              <% else %>
                <span><%= user.username.first.upcase %></span>
              <% end %>
            <% end %>

            <div class="fc-people-body">
              <div class="fc-people-head">
                <%= link_to user.display_name, user_path(user.username), class: "fc-people-name" %>
                <span class="fc-people-handle">@<%= user.username %></span>
              </div>

              <% if user.profile&.bio.present? %>
                <p class="fc-people-bio"><%= truncate(user.profile.bio, length: 180) %></p>
              <% end %>

              <div class="fc-people-meta">
                <span><%= pluralize(user.posts.published.count, "breath") %></span>
                <span class="fc-people-dot">·</span>
                <span><%= pluralize(user.followers_count, "follower") %></span>
                <% if user.profile&.location.present? %>
                  <span class="fc-people-dot">·</span>
                  <span><%= user.profile.location %></span>
                <% end %>
              </div>
            </div>

            <div class="fc-people-actions">
              <%= render "users/follow_actions",
                         user: user,
                         follow_class: "fc-btn fc-btn-primary",
                         following_class: "fc-btn" %>
            </div>
          </li>
        <% end %>
      </ul>

      <% if @pagy.pages > 1 %>
        <div class="mt-8">
          <%== pagy_nav(@pagy) %>
        </div>
      <% end %>
    <% else %>
      <div class="mystic-panel p-10 text-center">
        <h2 class="text-xl font-black text-[var(--fc-ink)]">No people found</h2>
        <p class="mystic-copy mx-auto mt-2 max-w-md">Try a username, location, ministry background, or a broader keyword.</p>
      </div>
    <% end %>
  </div>
</div>
